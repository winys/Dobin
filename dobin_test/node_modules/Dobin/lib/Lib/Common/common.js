module.exports = {
	utils : (function () {
		var util = ["Cookie","Http"];

		if( $.Config("mysql") )
			util.push("Mysql");

		if( $.Config("redis") )
			util.push("Redis");

		if( $.Config("session") )
			util.push("Session");
		
		return util;
	})()
}